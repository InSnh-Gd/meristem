route:
  receiver: meristem-operations-webhook
  group_by: [alertname, service]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: meristem-operations-webhook
    webhook_configs:
      - url: ${MERISTEM_ALERT_WEBHOOK_URL}
        send_resolved: true
